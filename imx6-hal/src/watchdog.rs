// TODO - finish this impl

/*
use crate::pac::wdog::*;

pub struct Watchdog<WDOG: AsRef<RegisterBlock> + AsMut<RegisterBlock>> {
    wdt: WDOG,
}

impl<WDOG: AsRef<RegisterBlock> + AsMut<RegisterBlock>> Watchdog<WDOG> {
    // NOTE: does not start/enable
    pub fn new(wdt: WDOG) -> Self {
        Watchdog { wdt }
    }
}
*/
